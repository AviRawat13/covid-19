<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/parent_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/view_padding_normal">

    <TextView
        android:id="@+id/developed_by"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@android:color/black"
        android:layout_marginTop="@dimen/view_margin_normal"
        android:layout_marginBottom="@dimen/view_margin_normal"
        android:textStyle="bold"
        tools:text="Developed by"/>

    <TextView
        android:id="@+id/api_data_from"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        tools:text="API Data from "/>

    <TextView
        android:id="@+id/news_data_from"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@android:color/black"
        android:layout_marginTop="@dimen/view_margin_normal"
        android:layout_marginBottom="@dimen/view_margin_normal"
        android:textStyle="bold"
        tools:text="News Data from "/>

</LinearLayout>